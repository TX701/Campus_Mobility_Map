Repository for the accessibility map application.

A workflow is called on push that updates the corresponding index.html, script.js, and style.css files in our project's Amazon S3 bucket.

An EC2 server is used to host the N8N workflows used in our project.
