{
  "name": "course-project",
  "version": "1.0.0",
  "scripts": {
    "build": "vite build",
    "deploy": "aws s3 sync ./dist s3://course-project-website --delete --acl public-read"
  },
  "dependencies": {
    "vite": "^2.5.0"
  },
  "devDependencies": {}
}